import { YearBeforePipe } from './year-before.pipe';

describe('YearBeforePipe', () => {
  it('create an instance', () => {
    const pipe = new YearBeforePipe();
    expect(pipe).toBeTruthy();
  });
});
